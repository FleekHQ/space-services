{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Create Identity",
  "required": [
    "username",
    "publicKey"
  ],
  "properties": {
    "username": {
      "type": "string",
      "title": "Username",
      "description": "Username may only contain alphanumeric characters or single hyphens, and cannot begin or end with a hyphen.",
      "pattern": "^[a-zA-Z\\d](?:[a-zA-Z\\d]|-(?=[a-zA-Z\\d])){0,38}$"
    },
    "publicKey": {
      "type": "string",
      "title": "Public Key",
      "description": "Public key generated from ed25519 algorithm (without 0x prefix, 72 characters long)",
      "pattern": "^[a-fA-F\\d]{72}$"
    }
  }
}