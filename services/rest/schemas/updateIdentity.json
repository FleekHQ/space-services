{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Update Identity",
  "anyOf": [
    { "required": ["username"] },
    { "required": ["displayName"] },
    { "required": ["avatarUrl"] }
  ],
  "properties": {
    "username": {
      "type": "string",
      "title": "Username",
      "description": "Username may only contain alphanumeric characters or single hyphens, and cannot begin or end with a hyphen.",
      "pattern": "^[a-zA-Z\\d](?:[a-zA-Z\\d]|-(?=[a-zA-Z\\d])){0,38}$"
    },
    "displayName": {
      "type": "string",
      "title": "Display name",
      "description": "Display name can have max 50 characters.",
      "pattern": "^.{0,50}$"
    },
    "avatarUrl": {
      "type": "string",
      "title": "Avatar URL"
    }
  }
}